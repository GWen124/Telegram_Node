name: Fetch and Test Nodes

on:
  schedule:
    - cron: '0 */6 * * *' 
  workflow_dispatch:      

jobs:
  run-all:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python Dependencies
        run: pip install requests pyyaml

      - name: Setup Cloudflare WARP
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update && sudo apt-get install cloudflare-warp -y
          warp-cli --accept-tos registration new
          warp-cli --accept-tos mode warp
          warp-cli --accept-tos connect
          sleep 5
          curl -4 ifconfig.me

      - name: Fetch TG Nodes and Generate API URL (fetch.py)
        run: python fetch.py

      - name: Setup Subconverter and Convert ALL Sources
        run: |
          wget https://github.com/tindy2013/subconverter/releases/latest/download/subconverter_linux64.tar.gz
          tar -zxvf subconverter_linux64.tar.gz
          ./subconverter/subconverter > /dev/null 2>&1 &
          sleep 2
          
          # 启动本地服务让 subconverter 能读取刚才抓的 TG 节点
          python -m http.server 8000 &
          sleep 1
          
          # 读取 fetch.py 生成的包含了所有外链和本地链接的 API
          API_URL=$(cat sub_api_url.txt)
          curl "$API_URL" > clash_nodes.yaml

      - name: Setup Mihomo
        run: |
          wget https://github.com/MetaCubeX/mihomo/releases/download/v1.18.3/mihomo-linux-amd64-v1.18.3.gz
          gunzip mihomo-linux-amd64-v1.18.3.gz
          mv mihomo-linux-amd64-v1.18.3 mihomo
          chmod +x mihomo

      - name: Global Dedup, Test and Rename Nodes (check.py)
        run: python check.py

      - name: Commit and Push final subscription
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Action"
          git add final_sub.yaml tg_nodes.txt
          git commit -m "Auto update valid nodes: $(date)" || echo "No changes to commit"
          git push
